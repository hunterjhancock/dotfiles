- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshenv:
    ~/.zshrc:
    ~/.gitconfig:

- create:
  - ~/Projects

- shell:
  - 
    command: ./setup_homebrew.zsh
    stdout: true
    stderr: true
